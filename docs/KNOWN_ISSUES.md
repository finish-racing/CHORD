# CHORD Known Issues / Remaining Refinement Targets

These are not blockers for packaging the current code snapshot, but they are areas likely to require refinement during the final Ubuntu test cycle:

- exact PostgreSQL runtime behavior still needs confirmation on target laptop
- export formats are currently JSON-first and may later expand
- OpenAI enhancement currently uses a conservative scaffold path
- duplicate/version/cover logic may need real-world tuning
- some UI/dashboard sections are intentionally data-dense and may benefit from refinement after laptop feedback
