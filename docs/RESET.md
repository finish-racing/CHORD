# CHORD Reset Guide

## Soft reset
Use soft reset to clear derived outputs while keeping the imported run inputs:
`RUN_ID=<id> bash installer/soft_reset.sh`

## Hard reset
Use hard reset to remove the run entirely:
`RUN_ID=<id> bash installer/hard_reset.sh`
